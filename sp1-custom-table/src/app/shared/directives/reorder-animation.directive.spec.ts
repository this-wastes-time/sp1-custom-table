import { ReorderAnimationDirective } from './reorder-animation.directive';

describe('ReorderAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ReorderAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
