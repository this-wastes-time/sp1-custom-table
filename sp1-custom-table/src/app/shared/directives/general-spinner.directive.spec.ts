import { GeneralSpinnerDirective } from './general-spinner.directive';

describe('GeneralSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneralSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
